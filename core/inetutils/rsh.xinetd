service shell
{
        flags                   = REUSE
        socket_type             = stream
        wait                    = no
        user                    = root
        server                  = /usr/sbin/rshd
        log_on_failure          += USERID
        disable                 = yes
}
