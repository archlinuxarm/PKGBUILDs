service sane-port
{
	port        = 6566
	socket_type = stream
	wait        = no
	user        = root
	group       = scanner
	server      = /usr/sbin/saned
	# disabled by default!
	disable     = yes
		  }
