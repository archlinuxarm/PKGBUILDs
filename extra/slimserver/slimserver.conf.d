# Configuration file for SlimServer on Arch Linux.
# Save this as /etc/conf.d/slimserver

# Enter space-delimited options on the following line.
# e.g. SLIMSERVER_OPTS="--diag --d_scan --d_command" 
# See "/home/slimserver/slimserver.pl --help" for a full list of possible options.
SLIMSERVER_OPTS=""
