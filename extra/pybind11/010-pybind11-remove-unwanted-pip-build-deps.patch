--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=42", "cmake>=3.18", "ninja"]
+requires = ["setuptools>=42"]
 build-backend = "setuptools.build_meta"
 
 
