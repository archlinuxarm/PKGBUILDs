post_install() {
  cat << EOF
GNOME-VFS support:
==> If you want to use GNOME Virtual FileSystem (experimental support):
==> 'pacman -S gnome-vfs'.
EOF
}

post_upgrade() {
  post_install $1
}

op=$1
shift

$op $*
