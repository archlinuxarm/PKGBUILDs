# arg 1:  the new package version
post_install() {
  echo ">>>"
  echo ">>> WARNING"
  echo ">>> -------"
  echo ">>> If you use French gateway submodule (SFR.pm) you"
  echo ">>> have to install the optional perl-date-manip package."
  echo ">>>"
}

# arg 1:  the new package version
# arg 2:  the old package version
post_upgrade() {
  post_install $1
}

op=$1
shift
$op $*
