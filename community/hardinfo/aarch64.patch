Index: b/configure
===================================================================
--- a/configure
+++ b/configure
@@ -66,6 +66,9 @@
 	sparc*)
 		ln -sf linux/sparc arch/this
 		ARCH="ARCH_SPARC" ;;
+	aarch64*)
+		ln -sf linux/aarch64 arch/this
+		ARCH="ARCH_AARCH64" ;;
 	armv*)
 		ln -sf linux/armv4l arch/this
 		ARCH="ARCH_ARMV4L" ;;
