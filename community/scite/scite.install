post_install() {
  echo "Updating desktop and mime database..."
  update-desktop-database -q
}

post_remove() {
  echo "Updating desktop and mime database..."
  update-desktop-database -q
}

# vim:set ts=2 sw=2 et:
